<div class="my-3">
  <canvas id="group_bar_chart"></canvas>
</div>

<script>
  var ctx = document.getElementById("group_bar_chart").getContext("2d");
  var jsonDataStr = `<%- JSON.stringify(lista, null, 2).replace(/"/g, '\\"') %>`;
  var jsonData = JSON.parse(jsonDataStr);
  console.log(jsonData.year);
  var chartData = "PromMaxMinXRegion".split(",").map(Number); 

  var data = {
    labels: jsonData.year.map((year, index) => `${year} - ${jsonData.region[index]}`),
    datasets: [
      {
        label: "Average",
        data: jsonData.average,
        backgroundColor: "rgba(54, 162, 235, 0.2)",
        borderColor: "rgba(54, 162, 235, 1)",
        borderWidth: 1,
      },
      {
        label: "Max",
        data: jsonData.max,
        backgroundColor: "rgba(75, 192, 192, 1)",
        borderColor: "rgba(75, 192, 192, 1)",
        borderWidth: 1,
      },
      {
        label: "Min",
        data: jsonData.min,
        backgroundColor: "rgba(153, 102, 255, 1)",
        borderColor: "rgba(153, 102, 255, 1)",
        borderWidth: 1,
      },
    ],
  };

  var options = {
    responsive: true,
    legend: {
      position: "top",
    },
  };

  // create the chart
  var chart = new Chart(ctx, {
    type: "bar",
    data: data,
    options: options,
  });
 </script>

