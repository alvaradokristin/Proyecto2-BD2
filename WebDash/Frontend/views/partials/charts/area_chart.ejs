<div class="my-3">
  <canvas id="area_chart"></canvas>
</div>

<script>
  var ctx = document.getElementById("area_chart").getContext("2d");
  var jsonDataStr = `<%- JSON.stringify(lista, null, 2).replace(/"/g, '\\"') %>`;
  var jsonData = JSON.parse(jsonDataStr);

  var dataLabels, dataLbl1, dataLbl12, chartData1, chartData2;

  if(jsonData.report == "PromVicXPaisXSexo"){
    dataLabels = jsonData.country;

    dataLbl = "Sexo";
    chartData = jsonData.sex;

    dataLbl12 = "Average";
    chartData2 = jsonData.average;
  }


  var myChart = new Chart(ctx, {
    type: "line",
    data: {
      labels: dataLabels,
      datasets: [
        {
          label: dataLbl1,
          data: chartData1,
          backgroundColor: "rgba(54, 162, 235, 0.2)",
          borderColor: "rgba(54, 162, 235, 1)",
          borderWidth: 1,
          fill: true,
        },
        {
          label: dataLbl12,
          data: chartData2,
          backgroundColor: "rgba(153, 102, 255, 1)",
          borderColor: "rgba(153, 102, 255, 1)",
          borderWidth: 1,
          fill: true,
        },
      ],
    },
  });
</script>
